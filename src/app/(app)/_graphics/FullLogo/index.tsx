import * as React from 'react'

export const FullLogo: React.FC<{ className?: string }> = ({ className }) => {
  return (
    <>
      <span className="font-logo text-2xl font-bold tracking-[-.06em]">{`thankly `}</span>
    </>
  )
}
